[package]
name = "scour-name"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

# Nothing. What a file may be called is a question about the filesystem, and
# the standard library is where the filesystem is.
[dependencies]
