[package]
name        = "scour-settings"
description = "What every frontend remembers between runs, kept where all of them can reach it."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
publish.workspace      = true

[lints]
workspace = true

# Serde, and nothing else. Not the index, not the engine, not a frontend:
# "which columns are shown" is a fact about a person, not about a filesystem.
[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
